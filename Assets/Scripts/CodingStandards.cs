using UnityEngine;

public class CodingStandards : MonoBehaviour
{
    // Start is called before the first frame update
    private void Start()
    {
        
    }

    // Update is called once per frame
    private void Update()
    {
        
    }

    /// <summary>
    ///     Sample of a public function, public functions require summaries
    /// </summary>
    /// <param name="number">
    ///     Used to pass along a number that has a default of zero
    /// </param>
    public void EpicFunction(int number = 0)
    {

    }
}
